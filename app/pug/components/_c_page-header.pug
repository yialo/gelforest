mixin page-header(currentPageId)
  header.page-header(class=[
    `page-header--${currentPageId}`,
    attributes.class,
    jsMenuClass,
    'no-js',
  ])
    include ../common/_test_services
    +logo(currentPageId).page-header__logo(class=`${jsMenuClass}__logo`)
    .page-header__call-button(class=`${jsMenuClass}__call-button`)
      +call-button()
    .page-header__menu-buttons
      +menu-buttons()
    +areas().page-header__areas(class=`${jsMenuClass}__container`)
    +interesting().page-header__interesting
